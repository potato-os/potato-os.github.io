using gui;
using Colour from graphics;

var window = gui.Window();
window.title("My Custom Window");

var container = gui.FlexBox();
container
	~> .fill()
	~> .direction("vertical")
	~> .gap(10)
	~> window.add_content();

for i in [0..5] {
	print("Adding to container!");
	var new = gui.ColourRect().random().expand(true);
	print(new);
	container.add(new);
}