using gui;
using Colour from graphics;

var window = gui.Window();
window.title("My Custom Window");
window.add_content(
	gui.Button()
		~> .margin_all(50)
		~> .text("Hello World!")
		~> .colour(Colour("#66ff00ff"))
);