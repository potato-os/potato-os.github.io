using gui;
using Colour from graphics;

var window = gui.Window();
window.title("My Custom Window");

var grid = gui.Grid()
	~> .columns(3)
	~> .gap_h(10)
	~> .gap_v(10)
	~> .fill();

for i in [0..9] {
	grid.add(gui.ColourRect().random().expand());
}