using gui;
using Colour from graphics;

var clicked = false;
var window = gui.Window();
window.title("My Custom Window");

var container = gui.FlexBox();
container
	~> .direction("vertical")
	~> .gap(10)
	~> window.add_content();

for _ in [0..5] {
	container.add(
		gui.ColourRect().random()
	);
}