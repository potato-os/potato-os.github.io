using gui;
using Colour from graphics;

var window = gui.Window();
window.title("My Custom Window");

function on_change(text) {
	print(text)
}

var text = gui.Input();
text
	~> .fill()
	~> .secret("*")
	~> .change(on_change)
	~> window.add_content();