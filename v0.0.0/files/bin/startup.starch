using gui;

var window = gui.Window();
window.add_content(
    gui.Text()
        ~> .fill()
        ~> .text("PotatoOS by [color=red]redis[/color][color=blue]notblue[/color]")
        ~> .align(1, 1)
);